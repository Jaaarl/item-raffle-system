<h1 class="bg-dark text-white">Admin home page</h1>
<% if user_signed_in? %>
  <h2> Hello <%= current_user.email %> </h2>
  <%= button_to 'Sign out', destroy_user_session_path, method: :delete %>
<% else %>
  <%= link_to 'Sign in', new_admin_session_path %>
<% end %>