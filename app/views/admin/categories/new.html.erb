<%= render 'admin/shared/sidebar' %>

  <div class="container-fluid mt-4">
    <h1>New Category</h1>
    <%= render partial: 'form', locals: { item: @category, custom_url: admin_categories_path } %>
  </div>
</div>